services:
  app:
    build: .
    container_name: yggdrasil-service
    restart: unless-stopped
    ports:
      - "8090:8080"
    volumes:
      - ../DataStorage/calendar:/data/calendar/
      - ../DataStorage/users.csv:/data/users.csv